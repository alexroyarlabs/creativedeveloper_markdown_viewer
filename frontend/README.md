# Frontend (React)

## Local development

```bash
npm install
npm run dev
```

## Configuration

Set the API base URL in `.env`:

```
VITE_API_BASE=http://localhost:8000
```

## URL usage

- `/projects/{projectId}`
- `/?projectId={projectId}`
